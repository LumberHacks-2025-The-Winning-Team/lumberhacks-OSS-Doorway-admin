name: Unit Test Feature Branch 2

on:
  push:
    branches: [fix-two-bugs]

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run overdraft/type bug unit test
        run: pytest tests/test_fix_two_bugs.py
